services:
  web:
    build: frontend/.
    ports:
      - "80:3000"
    volumes:
      - ./frontend:/frontend
  api:
    build: backend/.
    ports:
      - "3333:3333"
    volumes:
      - ./backend:/backend
